// takes in an KeyBoardShortcut and register it into the OS.

use std::fmt::Error;

use crate::models::hotkey::KeyBoardShortcut;

fn registerShortcut(shortcut: KeyBoardShortcut) -> Result<(), Error> {
    return Ok(());
}
