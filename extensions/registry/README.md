# Registry Catalog

`catalog.v1.json` is generated by the extension publish workflow.

Do not hand-edit catalog package URLs, hashes, sizes, or entries. Add or update
extension source under `packages/<id>/`, then let GitHub Actions publish the
package and commit the refreshed catalog metadata.
