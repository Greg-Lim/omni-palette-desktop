{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Phase 2 wireframe window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
